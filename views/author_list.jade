extends layout

block content
  h1= title

  ul
    each author in listOfAuthors
      li
        a(href=author.url) #{author.name}
        | (#{author.lifespanFormatted})

    else
      li There are no authors